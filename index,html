<!DOCTYPE html>
<html lang="{{ lang }}">
<head>
  <meta charset="UTF-8">
  <title>{{ t['title'] }}</title>
  <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
</head>
<body>
<div class="container">
  <h1>{{ t['title'] }}</h1>

  <!-- language switch -->
  <div class="lang-switch">
    <div class="dropdown">
      <button class="dropbtn">🌐</button>
      <div class="dropdown-content">
        <a href="/setlang/en">🇬🇧 English</a>
        <a href="/setlang/ru">🇷🇺 Русский</a>
        <a href="/setlang/az">🇦🇿 Azərbaycan</a>
      </div>
    </div>
  </div>

  <div class="input-group">
    <input type="password" id="password" placeholder="{{ t['enter_password'] }}">
    <button id="toggle">{{ t['toggle'] }}</button>
    <button id="copy">{{ t['copy'] }}</button>
  </div>

  <div class="btn-row">
    <button id="check">{{ t['check'] }}</button>
    <button id="generate">{{ t['generate'] }}</button>
    <button id="last5">{{ t['last5'] }}</button>
  </div>

  <div class="progress">
    <div id="progress-bar">0%</div>
  </div>

  <p id="time"></p>
  <p id="breach"></p>

  <h3>{{ t['recommendations'] }}</h3>
  <ul id="recs"></ul>

  <div id="history-box" class="hidden">
    <h3>{{ t['history'] }}</h3>
    <ul id="history"></ul>
    <button id="clear">{{ t['clear'] }}</button>
  </div>
</div>

<!-- theme toggle -->
<div class="theme-toggle">
  <span id="theme-btn">🌙</span>
</div>

<script src="{{ url_for('static', filename='js/script.js') }}"></script>
</body>
</html>
